lista=[]
while (true){
    var num=prompt("Ingresa numero: ");
    if (num==0)
        break;
    else
        lista.push(num);
 }
lista.sort();
console.log(lista);
/*for i in range(0,lista.__len__()):
    for j in range (0,lista.__len__()):
        if lista[j]>9
        lista[i]:
                temp=lista[i]
                lista[i]=lista[j]
                lista[j]=temp*/